PORT=8787
NODE_ENV=development
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_AGENT_MODEL=
DEFAULT_PROVIDER=openai-agents
# JSON array of virtual keys. Never commit production values.
AGENT_GATEWAY_KEYS=[{"id":"vk_dev","key":"ag_dev_local","tenantId":"tenant_dev","projectId":"project_dev"}]
# Optional path to a JSON config with a `channels` array.
# AGENT_GATEWAY_CONFIG=./config/channels.example.json
