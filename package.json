{
  "name": "agent-gateway",
  "version": "0.4.1",
  "private": true,
  "description": "Open-source Agent API gateway for routing, tenancy, session affinity, metering and billing across agent runtimes.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "test": "npm run build && node --test packages/core/dist/index.test.js packages/credential-crypto/dist/index.test.js packages/control-plane-auth/dist/index.test.js packages/storage-postgres/dist/index.test.js packages/runtime-redis/dist/index.test.js",
    "dev": "npm run build && node apps/server/dist/index.js",
    "start": "node apps/server/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.8.3"
  },
  "engines": { "node": ">=22" }
}
